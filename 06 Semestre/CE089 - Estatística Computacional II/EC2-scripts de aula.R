Ver formas de acesso para roda o R no servidor c3sl.

x<-1:1000000

st1 <- system.time({
    out1 <- numeric(length(X)
    for (i in 1:length(x)){
        out1[i] <- x²
    }
})
    
    >>>>> PEDIR OS SLIDES PARA O PROFESSOR <<<<<

      
      
search()    